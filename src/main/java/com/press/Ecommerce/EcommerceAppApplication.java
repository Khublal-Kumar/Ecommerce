package com.press.Ecommerce;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class EcommerceAppApplication {

	public static void main(String[] args) {
		SpringApplication.run(EcommerceAppApplication.class, args);
	}

}




/*
 * package com.press.Ecommerce; // This defines the package for the class, organizing it within a folder structure.

import org.springframework.boot.SpringApplication; // Imports the SpringApplication class to launch the application.
import org.springframework.boot.autoconfigure.SpringBootApplication; // Imports the annotation for automatic configuration.

@SpringBootApplication // This annotation indicates that this is a Spring Boot application.
public class EcommerceAppApplication { // Defines the main class for the e-commerce application.

    public static void main(String[] args) { // The main method, where the program starts executing.
        SpringApplication.run(EcommerceAppApplication.class, args); // Starts the Spring Boot application.
    }

}
 * 
 * 
 */
